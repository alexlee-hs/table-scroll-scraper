# table-scroll-scraper
Intake a video of a scrolling table and output a CSV.

# Install

```
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
pip install -e .
```

# Run

```
python -m scraper
python -m scraper --date 20260422
python -m scraper --debug
```
